<!-- expect: string[]; thinking: off; takeLast -->

<!-- let numberToGenerate = ceil(34 + floor(random() * 4)) -->

<!-- let maxWords = 2 + round(random() * 2) -->

What should I name my band? Generate me an entirely new and unique name for my new band that doesn't exist yet. The names you generate have a max of ${maxWords} words. Reply with a list of ${numberToGenerate}, in json array of the name alone, do not pre or post amble or generate anything other than the raw Title Case name you have arrived at
